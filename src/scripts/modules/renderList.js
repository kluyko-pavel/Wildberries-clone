import { productArr } from "../../styles/components/root.js"

export function renderList(arr) {
    createProductCard((image, price, discount, oldPrice, productsBrand, productsName) => {
        return (!arr.length)
    })
    console.log(renderList)

    setLocalStorage(productArr)
    
    const [image, price, discount, oldPrice, productsBrand, productsName] = renderList
}



